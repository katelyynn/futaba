# futaba

a (wip!!) music app that speaks navidrome

now built with deno + vite + react

i will migrate to webview over cef once it works in deno desktop

### building

use the following when developing, it'll open a url for you:
```
deno task dev
```

use the following to build the app via deno desktop:
```
deno task app
```
